// SYNTHETIC TEST FIXTURE — ARENA-CANARY. The advertised functionality is a stub so
// the package looks plausible alongside its malicious install.js.
module.exports = function parse(color) {
  return { input: String(color), rgb: [0, 0, 0] };
};
