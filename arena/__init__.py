"""Supply Chain Scanner Arena — a labeled corpus + scoring harness for supply-chain scanners."""

__version__ = "0.1.0"

# Marker embedded in every malicious sample to prove it is a synthetic test fixture.
CANARY = "ARENA-CANARY"
